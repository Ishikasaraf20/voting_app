<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Add Question</title>
  </head>
  <body>
    <div
      class="bg-gradient-to-r from-red-500 via-blue-500 to-pink-500 font-sans text-2xl shadow-md"
    >
      <div class="flex flex-row justify-between pr-4">
        <h1 class="w-3.2/5.4 p-3.5 text-white">The Voting App</h1>
        <div class="flex flex-row justify-end">
          <div class="hover:bg-blue/20  cursor-pointer flex items-center">
            <a href="/elections/<%= electionId %>" class="text-white px-2">
              Back
            </a>
          </div>
          <div class="hover:bg-blue/20  cursor-pointer flex items-center">
            <a href="/elections" class="text-white px-2">
              All Elections
            </a>
          </div>
          <div class="hover:bg-blue/20  cursor-pointer flex items-center">
            <a href="/logout" class="text-white px-2">
              Logout
            </a>
          </div>
        </div>
      </div>
    </div>
    <div class="flex flex-col items-center">
      <div class="h-1/6 text-white">.</div>
      <div class="shadow-xl p-3.5 mt-3.4 rounded-md w-3.2/5.4 bg-purple-100">
        <h1 class="text-2xl font-bold mb-4">Add Question</h1>
        <form action="/questions" method="post">
          <div class="flex-auto">
            <label for="body">Question</label>
            <input
              type="text"
              name="title"
              id="title"
              class="rounded w-full p-2.6 border-2 border-purple-300"
            />
          </div>
          <div class="flex-auto">
            <label for="body">Description</label>
            <input
              type="text"
              name="description"
              id="description"
              class="rounded w-full p-2.6 border-2 border-blue-300"
            />
          </div>
          <input type="hidden" name="electionId" value="<%= electionId %>" />
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="flex flex-row justify-end w-full">
            <button
              class="bg-blue-400 p-2.6 rounded m-2 hover:bg-green-500 text-white"
              type="submit"
            >
              Add
            </button>
          </div>
        </form>
        <% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
            <%- include('error', {message: messages.error[i]}) %>
          <% } %>
        <% } %>
      </div>
    </div>
  </body>
</html>
